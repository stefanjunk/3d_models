# All heavy imports in scripts/ are guarded; the skill runs on the Python
# standard library and reports a reduced capability set when these are absent.
numpy>=1.26,<3
trimesh>=4.0,<6
PyYAML>=6.0,<7
matplotlib>=3.8,<4
