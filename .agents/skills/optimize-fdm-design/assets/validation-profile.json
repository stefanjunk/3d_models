{
  "schema_version": "1.0",
  "skill": "optimize-fdm-design",
  "artifact_roles": [
    {"id": "baseline-mesh", "kind": "mesh", "required": true},
    {"id": "candidate-mesh", "kind": "mesh", "required": true},
    {"id": "sweep-report", "kind": "report", "required": true},
    {"id": "gcode", "kind": "gcode", "required": true},
    {"id": "measurement-report", "kind": "report", "required": true},
    {"id": "autonomy-policy", "kind": "autonomy-policy", "required": false},
    {"id": "agent-approval-ledger", "kind": "approval-ledger", "required": false},
    {"id": "human-approval-ledger", "kind": "approval-ledger", "required": false}
  ],
  "checks": [
    {"id": "candidate-mesh-audit", "type": "mesh", "required": true, "artifact_roles": ["candidate-mesh"]},
    {"id": "protected-geometry-regression", "type": "mesh_compare", "required": true, "artifact_roles": ["baseline-mesh", "candidate-mesh"]},
    {"id": "candidate-envelope", "type": "external_report", "required": true, "artifact_roles": ["baseline-mesh", "candidate-mesh", "sweep-report"]},
    {"id": "slicer-gcode", "type": "gcode", "required": true, "artifact_roles": ["gcode"]},
    {"id": "measured-benefit", "type": "external_report", "required": true, "artifact_roles": ["gcode", "measurement-report"]},
    {"id": "stage-provenance", "type": "approvals", "required": false, "artifact_roles": ["autonomy-policy", "agent-approval-ledger", "human-approval-ledger"]}
  ],
  "manual_gates": [
    {"id": "strength-or-function-test", "kind": "physical", "required": true},
    {"id": "pareto-selection", "kind": "approval", "required": true}
  ],
  "release_policy": {"block_statuses": ["FAIL", "NOT_RUN", "REVIEW_REQUIRED"], "require_sha256": true, "require_fresh_external_reports": true}
}
