# Guarded imports: each script degrades to a reduced check when these are absent.
trimesh>=4.0,<6
scipy>=1.12,<2
scikit-image>=0.24,<1
# blender_render_views.py runs only inside Blender, which supplies bpy/mathutils.
