interface:
  display_name: "Step1X Image to 3D"
  short_description: "Auditable image-to-GLB generation for hybrid printable design"
  default_prompt: "Use $step1x-image-to-3d to turn an isolated image into auditable Step1X GLBs and integrate the best candidate into a functional printable design."
policy:
  products:
    - chatgpt
    - codex
    - api
    - atlas
  allow_implicit_invocation: true
