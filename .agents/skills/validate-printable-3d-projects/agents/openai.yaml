interface:
  display_name: Validate Printable 3D Projects
  short_description: Deterministic validation and release gates for printable 3D projects
  default_prompt: Validate this printable 3D project with machine-readable, fail-closed checks and report every PASS, FAIL, NOT_RUN, and REVIEW_REQUIRED gate.
