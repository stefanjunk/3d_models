rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Add per-operation ownership/tenant/role and field validation with Emulator tests.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
