interface:
  display_name: "Select FDM Mechanism"
  short_description: "Choose a mechanism from the local sample library"
  default_prompt: "Use $select-fdm-mechanism to select and adapt a mechanism."
