---
description: Execute one bounded CAD edit, calculation, or validation task
agent: cad-microtask
subtask: true
---

Load the `functional-3d-design` skill with the native skill tool.

Perform this bounded task:

$ARGUMENTS

Before editing, restate a compact contract with: objective, input files and parameters, allowed files, forbidden decisions, acceptance command, and output format. If the request lacks enough information to make the task bounded, report the missing fields instead of inventing them. Never change approved requirements, choose an unverified safety/load/material assumption, or approve the complete design. Run the stated acceptance command and return assumptions, files changed, test result, and remaining risks.
