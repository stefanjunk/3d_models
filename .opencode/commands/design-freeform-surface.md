---
description: Design or refine a smooth parametric aesthetic envelope while preserving exact hardpoints and FDM printability.
---
Load the `parametric-freeform-surfacing` skill. If mechanical interfaces, material, or slicer decisions are involved, also load `functional-3d-design`. If an existing dense AI/scan mesh must be preserved, also load `organic-mesh-functionalization`.

Create or update `surfacing-spec.yaml`, select the simplest valid curve/surface representation, generate editable source and deterministic exports, and run the required fairness, hardpoint, topology, tessellation, and printability checks.

User request:

$ARGUMENTS
