---
description: Add or replace parametric functional geometry in an existing organic mesh
agent: 3d-design
---

Route this request to the `organic-mesh-functionalization` workflow owner:

$ARGUMENTS

Inspect only enough to summarize the requested result. Obtain requirements and
concept-image approval and `DESIGN_INTAKE_PASS` before geometry work. Then
preserve the source, create/update `operation-plan.yaml`, establish coordinates
and ROI/protected/transition/keep-out zones, and produce topology,
protected-surface, section, printability, and physical-test evidence.
