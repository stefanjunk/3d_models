---
description: Add or replace parametric functional geometry in an existing organic mesh
agent: build
---

Load `organic-mesh-functionalization` and execute its staged workflow for:

$ARGUMENTS

Preserve the source, create/update `operation-plan.yaml`, establish coordinates and ROI/protected/transition/keep-out zones, select the least destructive tool route, generate functional geometry parametrically, and produce only outputs that can be validated in the current environment. Include topology, protected-surface, section, printability, and physical-test evidence.
