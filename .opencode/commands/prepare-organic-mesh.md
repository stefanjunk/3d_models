---
description: Inspect, orient, proxy, segment, and plan an organic mesh without performing the final destructive edit
agent: 3d-design
---

Route to `organic-mesh-functionalization`. For:

$ARGUMENTS

Inspect only enough to summarize the requirements. Obtain requirements and
concept-image approval and record `DESIGN_INTAKE_PASS` before producing the
operation plan. Then create a
baseline report, determine scale and coordinates, define
ROI/protected/transition/keep-out regions, estimate memory, create a proxy if
needed, and recommend a tool/integration route. Do not modify the archival
source or claim that ambiguous regions are safely segmented.
