---
description: Inspect, orient, proxy, segment, and plan an organic mesh without performing the final destructive edit
agent: build
---

Load `organic-mesh-functionalization`. For:

$ARGUMENTS

Create a baseline report, determine scale and coordinates, identify mesh defects and components, define ROI/protected/transition/keep-out regions, estimate memory, create a proxy if needed, and recommend a tool/integration route. Do not modify the archival source or claim that ambiguous regions are safely segmented.
