---
description: Select and adapt a suitable FDM mechanical sample from the local 120-model library
---

Load the `fdm-mechanical-sample-library` skill. Analyze the requirement in `$ARGUMENTS` for degrees of freedom, permanent versus reusable connection, motion range, load class, material, printer, hardware allowance, desired play, and assembly direction. Search the local catalog, compare at least two mechanism families and all relevant tolerance variants, then recommend exact sample IDs and an integration/test workflow. Do not claim physical strength without a test plan.
