rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Add owner, size, type, path, lifecycle, and higher-risk scanning controls.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
