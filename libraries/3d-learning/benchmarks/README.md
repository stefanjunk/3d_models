# Benchmark store

Put versioned parametric coupon sources in `models/` and exact process results in
`measurements/`. Each result records coupon revision/hash, machine, material,
nozzle, slicer/profile, method, uncertainty, raw evidence, and pass/fail.
