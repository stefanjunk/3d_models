# Eval store

Eval categories are `core`, `geometry`, `dfam`, `interfaces`, `visual`, and
`regression`. Prefer deterministic outcome checks, use calibrated visual or
physical rubrics where necessary, and link every eval to its originating lesson
and regression suite.
