# Pattern store

Reusable solution structures live in `mounting`, `lightweight`, `airflow`,
`mechanics`, and `enclosures`. A pattern identifies its problem, use/avoid
conditions, parameterization, failure modes, variants, evidence, and evals.
New patterns start as candidates using `../templates/pattern.yaml`.
