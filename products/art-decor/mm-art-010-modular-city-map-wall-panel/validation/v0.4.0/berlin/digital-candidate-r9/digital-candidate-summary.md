# MM-ART-010 Berlin 0.4.0 — digital candidate r9

Status: **DRAFT digital pass; not a human print candidate or commercial release**.

Both requested examples exist as two native four-volume Anycubic project 3MFs. The source, mode-specific interface placements, 16 serialized color bodies, four reconstructed composites, aperture connectivity and four exact Anycubic imports/slices pass their digital gates. Each native slice reports 23 canonical layers, tools 0–3 and exactly three tool changes.

The generic standard-only 3MF parser fails because it does not follow Anycubic's object-part relationship layout; the destination slicer imports the same four projects and writes successful `result.json` plus non-empty G-code. The central G-code analyzer also double-counts 19 supplemental Vendor layer comments; the product-local canonical review resolves all four runs as PASS without modifying manufacturing G-code.

`boundary_crop` removes 58.34% of the modeled base area and reduces positive extrusion by 56.29% and estimated machine time by 54.88% relative to `context_outline`. This is a digital same-profile comparison; final purge settings and physical waste remain unqualified.

Open gates are exact filament/ACE selection, purge and layer GUI review, process-matched coupon, physical prints, assembly and wall proof, lit/unlit appearance, watermark, rights, safety and commercial release. No printer upload or print start was performed.
