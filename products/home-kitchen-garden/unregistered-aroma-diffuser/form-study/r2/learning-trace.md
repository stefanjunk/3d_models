# Scoped learning trace — E0, no promotion

This continues the existing appearance-priority eval candidate:
../../concepts/r1/learning/EVAL-core-aroma-appearance-priority-001.yaml.
The user directs parametric or Step1X creation and then says “contunie”.
No new physical measurement or appearance approval was provided.

Observed in this exact project:

- A cubic-profile radial loft is editable and closed, but its notched rim
  reads as a cut shell rather than the softly bundled crown in the AI hero.
- A deeper rounded rib field improved readability between Runs 001/002,
  while the independent reviewer still found the crown and belly inadequate.
- The image tool returned an RGB painted checkerboard despite a transparent
  request. Pixel-mode inspection rejected it; a white-background plate was
  generated separately for the existing service's rembg route.
- Existing container client 1.4.2 avoids a host dependency installation.
  A shallow copy path triggered the skill client's parents[5] assumption;
  retaining its directory depth resolved execution without a tool edit.
- Step1X produced a closer silhouette but ragged/merged ribs and four mesh
  components. The exact raw proposal was retained, not used as a production shell.
- The bundled-petal parameterization in Run 003 removed the cut-rim appearance;
  the read-only reviewer selected it as the best current development basis.

Candidate explanation, not established rule: for this appearance-first flame
motif, parametric control alone does not prove visual fidelity; actual multi-
view renders should select the envelope representation. Keep exact purchased
interfaces separate whichever exterior wins.

Scope: Blender 5.2.0, this source/parameters, unqualified virtual ivory
material, no actual filament/nozzle/profile/print/coupon. No production skill
or shared validated pattern changed. Potential tooling eval belongs on main.
