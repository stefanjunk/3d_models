# MM-ORG-022 — Labelable small-parts bin set

Draft digital print candidate derived from SKU-023 (opportunity score 88.8).

The original raster-free system supplies 45, 67.5 and 90 mm wide ScoopLabel bins, all 75 × 36 mm in body depth/height, plus a 186 × 156 mm open two-row registration frame and a 0.5/0.7/0.9 mm paper-label slot gauge. The bins tile two exact 180 mm row recipes for unpowered electronics and adult sewing notions.

Source of truth: `config/model-parameters.json` and `cad/build.py`. Rebuild with `python cad/build.py`; render with `python cad/render_preview.py`.

STEP, STL and 3MF outputs are DRAFT artifacts. They are not physically validated and make no child-use, battery, energized-parts, ESD, food, transport, spill, stacking-load or named-system compatibility claim.
