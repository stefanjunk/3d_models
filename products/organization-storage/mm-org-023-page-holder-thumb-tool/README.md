# MM-ORG-023 — Page-holder thumb tool

Draft S/M/L digital candidate derived from SKU-053 (opportunity score 88.8).

The original SoftBridge family supplies 20 × 16.5, 23 × 19 and 26 × 21.5 mm obround openings with 82, 92 and 102 mm spans. Each holder has fully rounded body/opening edges and two local 24 × 12 × 0.8 mm page pads. A separate sizing guide repeats all three openings.

Source of truth: `config/model-parameters.json` and `cad/build.py`. Rebuild with `python cad/build.py`; render with `python cad/render_preview.py`.

STEP, STL and 3MF outputs are DRAFT artifacts. They are not physically comfort-tested and make no medical, therapeutic, accessibility, universal-fit, child-use, binding-protection, paper-protection or archival claim.
