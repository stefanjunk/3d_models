# MM-ORG-024 — Deep-shelf pull-tab bin front

Draft digital candidate derived from SKU-118 (opportunity score 88.8).

The BridgeKey system separates one 120 × 48 mm pull/label face from measured 2.2, 2.9 and 3.6 mm PETG U-clips. A gap gauge and key-slot coupon make host mismatch fail before the full face is printed. The 2.9 mm preset is the bounded candidate for the 2.67 mm front rim of MM-ORG-002.

Source of truth: `config/model-parameters.json` and `cad/build.py`. STEP, STL and 3MF outputs remain DRAFT artifacts. The accessory is only for sliding a light bin forward on a shelf; it is not a carry handle and has no qualified load rating.
