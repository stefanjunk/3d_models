# Decision log

- Selected SKU-156 after reconciling MM-ORG-009 through MM-ORG-032 with the stale implementation overlay.
- Chose a rearward fall rather than a forward display angle because the primary gate is spill resistance.
- Split the front retention into two tabs so the center remains available for a label, finger or pour spout.
- Selected a one-piece side-print orientation to avoid fasteners, fit interfaces and support material.
- Kept the reference tray envelope generic and measurable; no compatibility with a named tray brand is claimed.
- Replaced face-touching shelf/rail/tab junctions with 0.4 mm volumetric fusion overlap after release-profile mesh audits found 12 rack and 2 coupon non-manifold edges despite valid one-solid B-Reps.
- Preserved slicer run 001 (relative-path failure) and run 002 (bed-origin exclusion-zone failure); run 003 uses absolute paths and a 20 mm build-item margin and passes.
- Preserved the superseded retry ledger after its out-of-order replay failed validation; `validation/agent-approvals-current.json` is the clean current chain for the final artifact.
